[project]
name = "argus-agents"
version = "0.1.0"
description = "Argus agents: research, master, persona, critic, synthesizer on a flat asyncio orchestrator"
requires-python = ">=3.12"
dependencies = [
    "argus-core",
    "argus-extraction",
    "argus-retrieval",
    "pydantic>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/argus_agents"]
